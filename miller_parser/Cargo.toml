[package]
name = "miller_parser"
version = "0.1.0"
edition = "2024"

[dependencies]
tree-sitter = "0.20"
tree-sitter-rust = "0.20"
serde = { version = "1.0", features = ["derive"] } # Aage VectorDB mein bhejne ke kaam aayega
